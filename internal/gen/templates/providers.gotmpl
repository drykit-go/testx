package check

var (
	{{- range .Vars -}}
	{{if ne .Name "Number" -}}
	{{range .DocLines}}
	// {{.}}
	{{- end}}
	{{.Name}} = {{.Value}}
	{{- end}}
	{{- end}}
)

// Map provides checks on type map[Key]Val.
func Map[Key comparable, Val any]() MapCheckerProvider[Key, Val] {
	return MapCheckerProvider[Key, Val]{}
}
