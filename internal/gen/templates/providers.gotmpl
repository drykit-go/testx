package check

type (
	{{- range .Interfaces -}}
	{{range .Docs}}
	// {{.}}
	{{- end}}
	{{.Name}} interface {
		{{range .Embedded -}}
		{{.}}

		{{end -}}
		{{range .Funcs -}}
		{{range .Docs -}}
		// {{.}}
		{{end -}}
		{{.Sign}}
		{{end}}
	}
	{{end}}
)

var (
	{{range .Vars -}}
	// {{.Name}} implements {{.Type}}.
	{{.Name}} {{.Type}} = {{.Value}}
	{{end}}
)
